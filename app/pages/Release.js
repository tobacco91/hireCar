import React, {Component} from 'react';
import {Platform, StyleSheet, Text, View} from 'react-native';
export default class Release extends Component {
    render() {
        return(
            <Text>Welcome to React Native!Release</Text>
        );
    }
}